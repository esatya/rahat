model Project {
  id        Int     @id @default(autoincrement())
  title     String
  isApproved Boolean @default(true)
  projectManager   User    @relation(fields: [projectManager], references: [id])
}